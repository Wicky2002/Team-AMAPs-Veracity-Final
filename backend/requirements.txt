fastapi==0.110.0
# starlette/uvicorn/sse-starlette are pinned explicitly here (normally just
# transitive deps): mcp>=2 requires uvicorn>=0.31.1 and pulls in sse-starlette
# by default, whose latest release needs starlette>=0.49 -- incompatible with
# fastapi 0.110's starlette<0.37 requirement. We only use mcp's stdio
# transport (no HTTP/SSE), so an old sse-starlette + old starlette is fine;
# uvicorn just serves ASGI and isn't starlette-version-sensitive.
starlette==0.36.3
uvicorn==0.34.0
sse-starlette==3.0.0
mcp==2.1.1
python-dotenv==1.0.1
pydantic==2.13.2
openai==2.32.0
anthropic==0.96.0
instructor==1.15.1
httpx>=0.26,<0.28
langgraph==1.1.2
langchain-ollama==1.1.0
beautifulsoup4==4.14.2
sentence-transformers==5.4.1
langgraph-checkpoint-postgres==3.0.5
psycopg[binary]==3.3.3